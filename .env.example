# Local development values only. Do not reuse these credentials outside a local machine.
POSTGRES_DB=rikkaus
POSTGRES_USER=rikkaus
POSTGRES_PASSWORD=rikkaus-local-only
POSTGRES_PORT=5432
DB_URL=jdbc:postgresql://localhost:5432/rikkaus
